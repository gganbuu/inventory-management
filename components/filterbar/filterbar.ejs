<aside class="filterBarContainer">
    
    <% const { devices = [], brand = [], priceFrom = "", priceTo = "" } = locals.search || {} %>
    <form action="/products" method="get">
        <h2>Devices</h2>
        <% for (const deviceName of locals.filters.devices) { %>
                <%- include("../labelCheckbox/labelCheckbox", {label: deviceName, filter: 'devices', checked: devices}) %>
        <% } %>

        <h2>Brand</h2>
        <% for (const brandName of locals.filters.brand) { %>
                <%- include("../labelCheckbox/labelCheckbox", {label: brandName, filter: 'brand', checked: brand}) %>
        <% } %>

        <h2>Price</h2>
        <label for="priceFrom">From</label>
        <input type="number" step="0.01" id="priceFrom" value="<%= priceFrom %>" name="priceFrom"/>
        <label for="priceTo">To</label>
        <input type="number" step="0.01" id="priceTo" value="<%= priceTo %>" name="priceTo"/>

        <button>Filter</button>
    </form>
</aside>